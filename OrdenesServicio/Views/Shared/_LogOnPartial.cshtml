@if(Request.IsAuthenticated) {
    <text>@Resources.SOResource.Usuario <strong>@User.Identity.Name</strong> 
    [ @Html.ActionLink(Resources.SOResource.CambiarContrasena, "ChangePassword", "Account") | @Html.ActionLink(@Resources.SOResource.Salir, "LogOff", "Account")  ]</text>
}
else {
    @:[ @Html.ActionLink(Resources.SOResource.IniciarSesion, "LogOn", "Account") ]
}
